spring.application.name=toggle
# MySQL adatbázis beállítások
spring.datasource.url=jdbc:mysql://localhost:3306/userdb?useSSL=false&serverTimezone=UTC
spring.datasource.username=test
spring.datasource.password=asdfg

# JDBC Driver
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA beállítások (adatbázis táblák automatikus létrehozása/frissítése)
spring.jpa.hibernate.ddl-auto=update
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

# Konzolon megjelenő SQL lekérdezések logolása (opcionális)
spring.jpa.show-sql=true
